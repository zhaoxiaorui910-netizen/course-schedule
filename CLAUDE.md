# 迭代与文件管理规范

## 核心原则
为了保持项目目录整洁，Claude 在修改或生成新版代码时必须遵循以下文件管理流程：

## 文件操作流程
1. **识别主文件**：本项目的核心文件应命名为简洁的固定名称，例如 `index.html`、`style.css` 和 `script.js`。
2. **归档旧版本**：
   - 在进行重大修改前，如果当前目录下已存在同名文件或旧版本文件（如 `course_v1.html`），请先将其移动到 `./archive/` 文件夹内。
   - 归档命名格式：`YYYYMMDD_HHMM_[原文件名]`。
3. **写入新版本**：
   - 始终将最新的代码写入主文件名（如 `index.html`），确保当前工作目录始终只有一份处于激活状态的代码。
   - **禁止** 在主目录下直接生成类似 `index_v2.html`、`index_final_v3.html` 的冗余文件。

## 修改前备份
1. 每次修改 `.py`、`.html`、`.js`、`.css` 等源代码文件前，先将当前版本备份到 `./archive/` 目录。
2. 备份命名格式：`YYYYMMDD_HHMM_[原文件名]`（例如 `20260508_1530_main.py`）。
3. 备份在修改前执行，确保即使一次错误改动也不会丢失之前的内容。
4. 小改动（仅改几行）同样需要备份：将原文件完整复制一份到 archive，再修改。

## 测试隔离
1. 涉及数据库增删改查的改动，必须使用隔离的测试数据库，禁止直接操作正式 `schedule.db`。
2. 启动测试模式：`COURSE_SCHEDULE_TEST=1 uvicorn main:app --host 0.0.0.0 --port 8014`，此时使用 `test.db`。
3. 正式模式：`uvicorn main:app --host 0.0.0.0 --port 8013`，使用 `schedule.db`。
4. 测试和正式可以同时运行，数据完全独立。代码共用同一份。
5. UI 样式/布局类改动不需要隔离环境，改完直接刷新验证即可。
1. 每次修复 bug 或调整逻辑后，向 `./archive/changelog.md` 追加一条记录。
2. 记录格式：
   ```
   YYYY-MM-DD HH:MM — [涉及文件]
   - 问题：<当时出了什么问题>
   - 原因：<根因分析>
   - 修正：<怎么改的>
   - 下次怎么做：<如何避免再犯>
   ```
3. 每次修复前先翻阅 changelog.md，检查是否之前踩过同样的坑。
4. 同样的错误不能出现第二次。

## 目录结构示例
保持如下布局：
.
├── archive/          # 存放所有历史版本 + changelog
├── index.html        # 最新的课程表网页
├── style.css         # 最新的样式
└── claude.md         # 本指令文件